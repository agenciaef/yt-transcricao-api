version: "3.8"

services:
  youtube-transcricao-api:
    image: ghcr.io/agenciaef/yt-transcricao-api:latest
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    deploy:
      replicas: 1
      restart_policy:
        condition: any